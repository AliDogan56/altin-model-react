{
  "name": "altin-model-react",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@11.19.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode localhost",
    "dev:localhost": "vite --mode localhost",
    "dev:development": "vite --mode development",
    "dev:production": "vite --mode production",
    "build": "node scripts/build-article-index.mjs && vite build && node scripts/generate-seo-pages.mjs",
    "build:localhost": "node scripts/build-article-index.mjs && vite build --mode localhost && node scripts/generate-seo-pages.mjs",
    "build:development": "node scripts/build-article-index.mjs && vite build --mode development && node scripts/generate-seo-pages.mjs",
    "build:production": "node scripts/build-article-index.mjs && vite build --mode production && node scripts/generate-seo-pages.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview",
    "articles:index": "node scripts/build-article-index.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "^7.18.2",
    "socket.io-client": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "sass": "latest",
    "typescript": "latest",
    "vitest": "^3.2.7"
  }
}
